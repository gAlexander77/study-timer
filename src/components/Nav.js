import React from 'react';

export default function Nav(){
    return(
        <div className="home">
            <h1>Study Timer</h1>
            <button>Timer Settings</button>
            <button>Stats</button>
        </div>
    );
}